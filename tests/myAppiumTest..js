const wd = require("wd");
const chai = require("chai");
const { assert } = chai;

// Set up Appium options
const serverConfig = {
    host: "localhost",
    port: 4723, // Default Appium port
};

describe("My Appium Test Suite", function () {
    before(async function () {});
});
